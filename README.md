# Image-identifying-webscraper
This is a web app that takes images as input, reads the image, categorizes the image. It then sends the identified object from image as parameters to the webscraper. The webscraper uses the parameters to search for the iobject online, gets the prices and compares them to find the cheapest price. The cheapest price is then returned to the user
